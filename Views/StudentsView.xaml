<UserControl x:Class="renkimsumatin.Views.StudentsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:renkimsumatin.Converters"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <!-- Smaller refresh button style (top-right, orange) -->
        <Style x:Key="RefreshButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="MinWidth" Value="60"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="Background" Value="#F59E0B"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Margin="10">
            <TextBlock Text="Student Management" FontSize="20" FontWeight="Bold" DockPanel.Dock="Left"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}" DockPanel.Dock="Right"
                    Style="{StaticResource RefreshButtonStyle}" VerticalAlignment="Top" HorizontalAlignment="Right"/>
        </DockPanel>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Student List -->
            <Border Grid.Column="0" Margin="0,0,10,0" Background="White" CornerRadius="5" Padding="10">
                <StackPanel>
                    <TextBlock Text="Student List" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <ListView ItemsSource="{Binding Students}" BorderThickness="0">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="150"/>
                                <GridViewColumn Header="Student ID" DisplayMemberBinding="{Binding StudentId}" Width="100"/>
                                <GridViewColumn Header="Actions" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="Edit" Command="{Binding DataContext.EditStudentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}" Margin="0,0,5,0" Padding="5,2"/>
                                                <Button Content="Delete" Command="{Binding DataContext.DeleteStudentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}" Padding="5,2" Background="#FF5252" Foreground="White"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </Border>

            <!-- Add Student Form -->
            <Border Grid.Column="1" Background="White" CornerRadius="5" Padding="10">
                <StackPanel>
                    <TextBlock Text="Add New Student" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="Student Name" Margin="0,0,0,5"/>
                    <TextBox x:Name="NameTextBox" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"
                             Padding="5"/>
                    <TextBlock Text="Student ID" Margin="0,0,0,5"/>
                    <TextBox x:Name="StudentIdTextBox" Text="{Binding StudentId, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"
                             Padding="5"/>
                    <Button Content="Add Student" Command="{Binding AddStudentCommand}"
                            Background="#4F46E5" Foreground="White" Padding="10,5"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Bottom note -->
        <Border Grid.Row="2" Background="Transparent" Margin="10,0,10,10">
            <TextBlock Text="Note: Student ID cannot be edited. Only Student Name can be edited." Foreground="#6B7280" HorizontalAlignment="Left"/>
        </Border>

        <!-- Edit Modal -->
        <Border Grid.RowSpan="3" Background="#80000000" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}">
            <Border Background="White" Width="400" CornerRadius="10" Padding="20">
                <StackPanel>
                    <TextBlock Text="Edit Student" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
                    <TextBlock Text="Student Name" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding EditingStudent.Name, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,10" Padding="5"/>
                    <TextBlock Text="Student ID" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding EditingStudent.StudentId, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,20" Padding="5" IsReadOnly="True"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Command="{Binding CancelEditCommand}" Margin="0,0,10,0" Padding="10,5"/>
                        <Button Content="Save Changes" Command="{Binding UpdateStudentCommand}"
                                Background="#4F46E5" Foreground="White" Padding="10,5"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</UserControl>