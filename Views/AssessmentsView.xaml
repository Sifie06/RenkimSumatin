<UserControl x:Class="renkimsumatin.Views.AssessmentsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:renkimsumatin.Converters"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:StringToBoolConverter x:Key="StringToBoolConverter"/>
        <!-- Smaller refresh button style (top-right, orange) -->
        <Style x:Key="RefreshButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="MinWidth" Value="60"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="Background" Value="#F59E0B"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Margin="10">
            <TextBlock Text="Assessment Management" FontSize="20" FontWeight="Bold" DockPanel.Dock="Left"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}" DockPanel.Dock="Right"
                    Style="{StaticResource RefreshButtonStyle}" VerticalAlignment="Top" HorizontalAlignment="Right"/>
        </DockPanel>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Assessment List -->
            <Border Grid.Column="0" Margin="0,0,10,0" Background="White" CornerRadius="5" Padding="10">
                <StackPanel>
                    <TextBlock Text="Assessment List" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <ListView ItemsSource="{Binding Assessments}" BorderThickness="0">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="150"/>
                                <GridViewColumn Header="Total Marks" DisplayMemberBinding="{Binding TotalMarks}" Width="100"/>
                                <GridViewColumn Header="Subject" DisplayMemberBinding="{Binding SubjectName}" Width="150"/>
                                <GridViewColumn Header="Actions" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="Edit" Command="{Binding DataContext.EditAssessmentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}" Margin="0,0,5,0" Padding="5,2"/>
                                                <Button Content="Delete" Command="{Binding DataContext.DeleteAssessmentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}" Padding="5,2" Background="#FF5252" Foreground="White"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </Border>

            <!-- Add Assessment Form -->
            <Border Grid.Column="1" Background="White" CornerRadius="5" Padding="10">
                <StackPanel>
                    <TextBlock Text="Add New Assessment" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="Assessment Name" Margin="0,0,0,5"/>
                    <TextBox x:Name="NameTextBox" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"
                             Padding="5"/>
                    <TextBlock Text="Total Marks" Margin="0,0,0,5"/>
                    <TextBox x:Name="TotalMarksTextBox" Text="{Binding TotalMarks, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"
                             Padding="5"/>
                    <TextBlock Text="Subject" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding Subjects}" DisplayMemberPath="Name" SelectedValuePath="Id"
                              SelectedValue="{Binding SelectedSubjectId}" Margin="0,0,0,10" Padding="5"/>
                    <Button Content="Add Assessment" Command="{Binding AddAssessmentCommand}"
                            Background="#4F46E5" Foreground="White" Padding="10,5"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Edit Modal -->
        <Border Grid.RowSpan="2" Background="#80000000" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}">
            <Border Background="White" Width="400" Height="350" CornerRadius="10" Padding="20">
                <StackPanel>
                    <TextBlock Text="Edit Assessment" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
                    <TextBlock Text="Assessment Name" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding EditingAssessment.Name, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,10" Padding="5"/>
                    <TextBlock Text="Total Marks" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding EditingAssessment.TotalMarks, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,10" Padding="5"/>
                    <TextBlock Text="Subject" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding Subjects}" DisplayMemberPath="Name" SelectedValuePath="Id"
                              SelectedValue="{Binding EditingAssessment.SubjectId}" Margin="0,0,0,20" Padding="5"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Command="{Binding CancelEditCommand}" Margin="0,0,10,0" Padding="10,5"/>
                        <Button Content="Save Changes" Command="{Binding UpdateAssessmentCommand}"
                                Background="#4F46E5" Foreground="White" Padding="10,5"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</UserControl>